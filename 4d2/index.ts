// db.connect(config)

// db.get('path' || `SELECT * FROM games WHERE 1=1`)

// db.watch('path' || `Query selectors with watchers is not available on our free plan`)

// db.put('path', payload, opts)

// db.delete('path', opts)
